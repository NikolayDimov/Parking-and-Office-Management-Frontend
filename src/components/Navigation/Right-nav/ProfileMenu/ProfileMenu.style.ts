import styled from 'styled-components';
import { DropdownMenu, Content } from '@radix-ui/react-dropdown-menu';

export const DropdownMenuStyled = styled(Content)`
    background-color: red;
    z-index: 20;
`;
